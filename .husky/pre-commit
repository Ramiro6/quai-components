echo "🔍 Verificando código antes del push..."

# Lint del código afectado
npx nx affected --target=lint --base=origin/main || exit 1

# Tests de las apps afectadas
# nx affected --target=test --base=origin/main || exit 1

# Build de las apps afectadas
# nx affected --target=build --base=origin/main || exit 1
npx nx run-many --target=build --all --parallel

echo "✅ Todas las verificaciones pasaron correctamente"
